export const USER_ROLES: Readonly<{
  USER: string;
  MOD: string;
}> = {
  USER: "user",
  MOD: "mod",
};

export const APPROVAL_STATUS: Readonly<{
  PENDING: string;
  APPROVED: string;
  SPAM: string;
}> = {
  PENDING: "pending",
  APPROVED: "approved",
  SPAM: "spam",
};
